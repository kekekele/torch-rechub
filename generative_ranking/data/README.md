Place dataset files for the standalone generative_ranking package here.

- MovieLens-1M expected files: generative_ranking/data/ml-1m/ratings.dat, movies.dat, users.dat

You can also override `data_dir` from the CLI in `python -m generative_ranking.train` or `python -m generative_ranking.infer`.